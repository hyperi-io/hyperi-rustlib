# A Rust requivalent of hs-lib

/projects/hs-lib (python package)
and
/projects/clickhouse-loader/pkg/hs-golib

Where it makes sense for rust
EXCEPT for the APP object types
golib probably has more up to date metrics, prometheus etc. but I'm not certain
Deployed through to artifactory (its own repo)
USe jf to create the repo in artifatory if a rust one is required